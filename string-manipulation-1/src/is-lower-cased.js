/* exported isLowerCased */

function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}

/* create function
use if statement to compare the string to itself but lowercased
is the same return true
else return false */
